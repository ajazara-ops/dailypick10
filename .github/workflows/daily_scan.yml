name: Daily Stock Scanner

on:
  schedule:
    # 1. 한국 시간 아침 08:00 (UTC 전날 23:00) -> 미국장 마감 -> 미국만 분석
    - cron: '0 23 * * 0-4'

    # 2. 한국 시간 저녁 18:00 (UTC 당일 09:00) -> 한국장 마감 -> 한국만 분석
    - cron: '0 9 * * 1-5'
    
    # 3. 한국 시간 토요일 아침 08:00 (UTC 금요일 23:00) -> 주간 리포트
    - cron: '0 23 * * 5'

  workflow_dispatch: # 수동 실행 버튼

permissions:
  contents: write

jobs:
  run-scanner:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas requests lxml

      - name: Run Scan Logic
        run: |
          # 현재 시간과 요일(UTC 기준) 확인
          HOUR=$(date +%H)
          DOW=$(date +%u)
          
          # 1. 토요일 아침 (금요일 23시) -> 주간 리포트 실행
          if [ "$DOW" -eq 5 ] && [ "$HOUR" -eq 23 ]; then
            echo "📅 Running Weekly Report Mode"
            python daily_stock_scanner.py --mode weekly
            
          # 2. 평일 아침 (일~목 23시) -> 미국 주식만 분석 (--target US)
          elif [ "$HOUR" -eq 23 ]; then
            echo "🇺🇸 Running US Stock Scan Mode"
            python daily_stock_scanner.py --mode daily --target US
            
          # 3. 평일 저녁 (월~금 09시) -> 한국 주식만 분석 (--target KR)
          else
            echo "🇰🇷 Running KR Stock Scan Mode"
            python daily_stock_scanner.py --mode daily --target KR
          fi

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          git add todays_recommendation.json
          git add history/*.json history_index.json || true
          
          git commit -m "Update Stock Data via GitHub Action" || echo "No changes to commit"
          git push
